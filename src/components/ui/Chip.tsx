export default function Chip({ children }: { children: any }) {
  return <span className='ds-pill mb-1.5 mr-1.5 border-[var(--border-default)] bg-[var(--surface-1)] text-[var(--text-secondary)]'>{children}</span>
}
