{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hippiescientist.app/schemas/source-candidate.schema.json",
  "title": "Source candidate review queue",
  "description": "Deterministic pre-registry candidate source records linked to governed intake tasks.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "candidateSourceId",
      "intakeTaskId",
      "title",
      "sourceType",
      "sourceClass",
      "evidenceClass",
      "publicationStatus",
      "proposedReliabilityTier",
      "duplicateRisk",
      "reviewStatus",
      "active"
    ],
    "properties": {
      "candidateSourceId": { "type": "string", "pattern": "^cand_[a-z0-9]+(?:-[a-z0-9]+)*$" },
      "intakeTaskId": { "type": "string", "pattern": "^intake_[a-z0-9_]+$" },
      "title": { "type": "string", "minLength": 1 },
      "shortTitle": { "type": "string", "minLength": 1 },
      "sourceType": {
        "type": "string",
        "enum": [
          "journal-article",
          "systematic-review",
          "meta-analysis",
          "clinical-trial-registry",
          "monograph",
          "regulatory-guidance",
          "reference-database",
          "book",
          "other"
        ]
      },
      "sourceClass": {
        "type": "string",
        "enum": [
          "randomized-human-trial",
          "non-randomized-human-study",
          "observational-human-evidence",
          "systematic-review-meta-analysis",
          "preclinical-mechanistic-study",
          "traditional-use-monograph",
          "regulatory-agency-monograph-guidance",
          "reference-database-authority"
        ]
      },
      "organization": { "type": "string", "minLength": 1 },
      "authors": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 },
        "uniqueItems": true
      },
      "publicationYear": { "type": "integer", "minimum": 1800, "maximum": 2100 },
      "citationText": { "type": "string", "minLength": 1 },
      "doi": { "type": "string", "pattern": "^10\\.[0-9]{4,9}\\/.+" },
      "pmid": { "type": "string", "pattern": "^[1-9][0-9]{4,8}$" },
      "canonicalUrl": { "type": "string", "format": "uri" },
      "monographId": { "type": "string", "minLength": 1 },
      "isbn": { "type": "string", "pattern": "^(?:97[89][- ]?)?[0-9][- 0-9]{8,}[0-9Xx]$" },
      "language": { "type": "string", "minLength": 2 },
      "jurisdiction": { "type": "string", "minLength": 2 },
      "evidenceClass": {
        "type": "string",
        "enum": [
          "human-clinical",
          "human-observational",
          "preclinical-mechanistic",
          "traditional-use",
          "regulatory-monograph"
        ]
      },
      "studyDesign": {
        "type": "string",
        "enum": [
          "randomized-controlled-trial",
          "non-randomized-trial",
          "cohort",
          "case-control",
          "cross-sectional",
          "systematic-review",
          "meta-analysis",
          "in-vitro",
          "in-vivo-animal",
          "narrative-monograph",
          "regulatory-guidance",
          "database-reference",
          "other"
        ]
      },
      "publicationStatus": {
        "type": "string",
        "enum": ["published", "preprint", "withdrawn", "superseded", "archived"]
      },
      "proposedReliabilityTier": { "type": "string", "enum": ["tier-a", "tier-b", "tier-c", "tier-d"] },
      "duplicateRisk": { "type": "string", "enum": ["low", "moderate", "high", "known-duplicate"] },
      "duplicateOfSourceId": { "type": "string", "pattern": "^src_[a-z0-9]+(?:-[a-z0-9]+)*$" },
      "reviewer": { "type": "string", "minLength": 1 },
      "reviewedAt": { "type": "string", "format": "date-time" },
      "reviewStatus": {
        "type": "string",
        "enum": [
          "draft_candidate",
          "needs_metadata",
          "under_review",
          "approved_for_registry",
          "rejected",
          "duplicate_of_existing",
          "deprecated_candidate"
        ]
      },
      "approvalNotes": { "type": "string" },
      "rejectionReasons": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 },
        "uniqueItems": true
      },
      "active": { "type": "boolean" }
    }
  }
}
