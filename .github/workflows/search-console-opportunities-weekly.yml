name: Weekly Search Console Opportunities

on:
  schedule:
    # Fridays at 07:20 UTC.
    - cron: '20 7 * * 5'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  search-opportunities:
    name: Pull Search Console data and rank opportunities
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      GSC_SERVICE_ACCOUNT_JSON: ${{ secrets.GSC_SERVICE_ACCOUNT_JSON }}
      GSC_SITE_URL: ${{ vars.GSC_SITE_URL || 'sc-domain:thehippiescientist.net' }}
      GSC_LOOKBACK_DAYS: '28'
      GSC_LAG_DAYS: '3'

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # The AI incident monitor inspects changes between the baseline window
          # and the detected dip. Keep enough history for that forensic check.
          fetch-depth: 60

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Require Search Console credentials
        run: |
          if [ -z "$GSC_SERVICE_ACCOUNT_JSON" ]; then
            echo "::error::Missing repository secret GSC_SERVICE_ACCOUNT_JSON. Add a Search Console service-account JSON key whose client email has read access to the property."
            exit 1
          fi

      - name: Fetch finalized Search Console performance
        run: node scripts/seo/fetch-search-console.mjs

      - name: Rank search opportunities
        run: npm run seo:opportunities

      - name: Measure August spam-update cohorts
        run: node scripts/seo/post-spam-cohort-monitor.mjs

      - name: Run anomaly-safe AI citation analytics
        run: node scripts/seo/run-ai-citation-analytics.mjs

      - name: Diagnose Bing AI citation incidents
        run: node scripts/seo/ai-citation-incident-monitor.mjs

      - name: Upload opportunity and cohort reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: weekly-search-opportunities-${{ github.run_number }}
          path: |
            ops/reports/search-opportunities.json
            ops/reports/search-opportunities.md
            ops/reports/post-spam-cohorts.json
            ops/reports/post-spam-cohorts.md
            ops/reports/ai-visibility-anomaly.json
            ops/reports/ai-visibility-anomaly.md
            ops/reports/ai-citations.json
            ops/reports/ai-citations.md
            ops/reports/ai-citation-incident.json
            ops/reports/ai-citation-incident.md
            ops/ai-citations/incidents/*.json
            data-sources/search-console/fetch-metadata.json
            data-sources/search-console/pages-by-date.csv
          retention-days: 60
          if-no-files-found: ignore

      - name: Publish report to workflow summary
        if: always()
        run: |
          echo "## Weekly Search Console Opportunities" >> "$GITHUB_STEP_SUMMARY"
          echo "- Property: $GSC_SITE_URL" >> "$GITHUB_STEP_SUMMARY"
          echo "- Window: 28 finalized days, ending 3 days before the run" >> "$GITHUB_STEP_SUMMARY"
          if [ -f ops/reports/search-opportunities.md ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat ops/reports/search-opportunities.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- Search opportunity report was not generated; inspect the failed step above." >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f ops/reports/post-spam-cohorts.md ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat ops/reports/post-spam-cohorts.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- Post-spam cohort report was not generated; inspect the failed step above." >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f ops/reports/ai-visibility-anomaly.md ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat ops/reports/ai-visibility-anomaly.md >> "$GITHUB_STEP_SUMMARY"
          fi
          if [ -f ops/reports/ai-citation-incident.md ]; then
            echo "" >> "$GITHUB_STEP_SUMMARY"
            cat ops/reports/ai-citation-incident.md >> "$GITHUB_STEP_SUMMARY"
          fi
