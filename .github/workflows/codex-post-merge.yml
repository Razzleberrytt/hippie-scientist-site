name: codex-post-merge

on:
  workflow_dispatch:

permissions:
  actions: write

jobs:
  post-merge-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "codex-post-merge is intentionally manual-only for bootstrap."
          echo "Enable automatic post-merge dispatch after the first successful codex-next cycle."
