name: Remediate audit lockfile

on:
  push:
    branches:
      - fix/audit-high-js-yaml-sharp
    paths:
      - package.json
      - .github/workflows/remediate-audit-lockfile.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh-lockfile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: fix/audit-high-js-yaml-sharp
          fetch-depth: 0

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Refresh lockfile for patched security releases
        run: npm install --package-lock-only --ignore-scripts --no-save js-yaml@3.15.2 sharp@0.35.4

      - name: Commit refreshed lockfile
        run: |
          if git diff --quiet -- package-lock.json; then
            echo 'No lockfile changes were produced.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package-lock.json
          git commit -m 'chore(security): refresh lockfile for patched dependencies'
          git push origin HEAD:fix/audit-high-js-yaml-sharp
