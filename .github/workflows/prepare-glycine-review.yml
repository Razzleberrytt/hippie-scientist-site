name: Prepare Glycine Review

on:
  push:
    branches:
      - data/glycine-evidence-review

permissions:
  contents: write

jobs:
  prepare:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: data/glycine-evidence-review
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci
      - run: npm run evidence:finalize -- --slug=glycine
      - run: |
          test -f data/reviews/evidence/glycine.report.md
          test -f data/reviews/evidence/glycine.report.json
          test -f data/reviews/evidence/glycine.manifest.json
      - run: rm .github/workflows/prepare-glycine-review.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/reviews/evidence/glycine.* .github/workflows/prepare-glycine-review.yml
          git commit -m "data: prepare glycine evidence review"
          git push origin HEAD:data/glycine-evidence-review
