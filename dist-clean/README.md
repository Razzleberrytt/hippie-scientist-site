# dist-clean reference

This folder is a placeholder to document deployment output expectations.

- Build artifacts are generated in `dist/` locally.
- `dist/` is intentionally git-ignored and must never be committed.
- No minified bundles, hashed assets, or binaries are stored here.
