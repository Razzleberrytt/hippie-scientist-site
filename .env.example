# Canonical endpoint for newsletter/contact/lead-capture form submissions.
# Runtime code in src/lib/formSubmission.ts reads this key first.
VITE_FORM_ENDPOINT=

# Legacy fallback keys (deprecated).
# Keep only while migrating old environments; remove after all environments use VITE_FORM_ENDPOINT.
VITE_LEAD_CAPTURE_ENDPOINT=
VITE_NEWSLETTER_ENDPOINT=
