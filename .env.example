# Optional: endpoint for lead/newsletter capture form submissions.
# If empty, submissions are saved locally in the browser.
VITE_LEAD_CAPTURE_ENDPOINT=

# Backward-compatible fallback key used in older environments.
VITE_NEWSLETTER_ENDPOINT=
