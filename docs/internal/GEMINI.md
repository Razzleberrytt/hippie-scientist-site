# GEMINI.md

## Development Instructions

- Use **functional React components** with **TypeScript** and **Tailwind CSS** for all UI work.
- **No legacy classes**: Do not introduce or use class-based React components.
- Always check `package.json` before adding new libraries or writing code that depends on packages.
- Before making changes that affect more than 3 files, always ask for explicit confirmation first.
