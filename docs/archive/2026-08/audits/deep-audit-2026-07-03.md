# Deep Audit Report — July 3, 2026

Source archive: `hippie-scientist-site-main 34.zip`  
Patched archive target: `hippie-scientist-site-main-34-deep-audited-clean.zip`

## Executive Summary

This pass focused on making the uploaded repository cleaner and safer to hand to Codex as a full replacement archive.

### High-confidence changes applied

- Removed the root `archive/` directory, which contained superseded plans, roadmaps, and historical handoff notes that were not part of the active runtime.
- Removed `legacy-quarantine/`, which contained excluded App Router and component code that was no longer part of active build/typecheck routes.
- Removed stale root replacement prompts and audit baselines that referenced older `zip 31` artifacts.
- Removed duplicate/superseded `ops/research/archive/` payloads and updated `ops/research/MASTER_RESEARCH_INDEX.md` to point at canonical retained files.
- Hardened Ahrefs analytics loading:
  - `app/layout.tsx` now routes Ahrefs through `next/script`.
  - `.env.example` documents `NEXT_PUBLIC_AHREFS_ANALYTICS_KEY`.
  - `public/_headers` now allows `https://analytics.ahrefs.com` in CSP `script-src` and `connect-src`.
  - `scripts/ci/validate-security-headers.mjs` now checks required analytics CSP origins, not only header names.
- Regenerated route inventory and retained the metadata duplicate audit report.

### Repository-size impact

- Original archive files: **3316**
- Updated archive files: **3164**
- Files removed: **152**
- Uncompressed size removed: **1.10 MB**

## Validation Performed

These checks were run successfully in the extracted repository:

```text
node scripts/ci/validate-security-headers.mjs
node scripts/ci/validate-quarantine-imports.mjs
node scripts/ci/validate-public-json-imports.mjs
node scripts/ci/validate-xlsx-boundary.mjs
node scripts/ci/validate-static-export-compatibility.mjs
node scripts/ci/validate-route-seo.mjs
node scripts/validate-route-governance.mjs
node scripts/ci/validate-guide-faqs.mjs
node scripts/ci/validate-dangerously-set-inner-html.mjs
```

Key results:

```text
Static export guard: PASS
Route governance: PASS — 204 app routes, no route conflicts
Route SEO validator: PASS — checked 204 app routes and 562 source files
Quarantine import boundaries: PASS
Security headers: PASS — required headers and analytics CSP origins present
Dangerous HTML injection guard: PASS
Guide FAQ internal taxonomy leak guard: PASS
```

## Findings Still Open

### 1. Full build/typecheck/lint could not be run inside this sandbox

The archive does not include `node_modules/`. The local dependency gate reports:

```text
Dependencies are not installed. Run: npm ci
Missing dependency artifacts: node_modules/, @content-collections/cli, eslint, exceljs, next, pagefind, react, react-dom, tsx, vitest, wrangler, ...
```

Run these after Codex replacement in a networked environment:

```bash
npm ci
npm run check:node
npm run typecheck
npm run lint:nocache
npm run build:deploy
npm run verify:output
```

### 2. Data governance warning remains diagnostic

`node scripts/ci/validate-data-governance.mjs` reports:

```text
[WARNING] INDEXABLE_COMPOUNDS_WITHOUT_SOURCES
Indexable compound profiles should be source-backed once compound-source ingestion is available.
```

This is not new in this cleanup. The warning currently covers 23 indexable compound profiles, including `acetyl-l-carnitine`, `alpha-gpc`, `berberine`, `caffeine`, `l-theanine`, `magnesium`, `omega-3`, and others.

### 3. Content-collections generated module is expected to exist only after generation

The active app import graph has five expected unresolved imports pointing at `.content-collections/generated`:

```text
app/articles/page.tsx
app/articles/[slug]/page.tsx
app/compounds/[slug]/page.tsx
app/novel-psychoactive-substances/page.tsx
app/novel-psychoactive-substances/[slug]/page.tsx
```

These are expected to be generated by:

```bash
npm run content:build
```

## Codex Replacement Recommendation

Use `CODEX_REPLACE_WITH_AUDITED_ZIP_PROMPT.md` with the final audited zip. The replacement command intentionally uses `rsync --delete`, which removes old files in the destination repository that are not present in the new archive.
