# Site Health delegation smoke test

This temporary docs-only change exists to exercise the non-release-sensitive pull-request path of the scoped Site Health workflow before the policy is merged to `main`.
