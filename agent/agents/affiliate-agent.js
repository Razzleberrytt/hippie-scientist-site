export function runAffiliateAgent() {
  return {
    preferred_forms: ['standardized extract'],
    quality_markers: ['third-party tested', 'transparent labeling'],
    avoid: ['proprietary blends'],
  }
}
